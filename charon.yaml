# charon.yaml 

# install: conda env create -f charon.yaml 
# update:  conda env update -f charon.yaml 
name: charon
dependencies:
  - python=3.7
  - pip
  - tensorflow-gpu=1.*
  - cudnn=7
  - wxpython
  - jupyter
  - nb_conda
  - Shapely
  - ipython
  - pandas
  - opencv
  - numpy
  - scipy
  - tqdm
  - protobuf
  - pip:
      - pillow
      - lxml
      - contextlib2
      - jupyter
      - matplotlib
      - pandas
      - opencv-python
      - pycocotools
      - tf_slim
